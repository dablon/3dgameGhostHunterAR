# Generated glTF assets

Populated by `npm run assets:export`. **Do not edit by hand** — re-run the
export to refresh after editing `.blend` sources.

The Vite dev server serves everything under `public/` at the URL root, so
files here are reachable at `/assets/models/<name>.glb`.
